syntax = "proto3";

option go_package = "module github.com/snooyen/animal-facts/facts";

package facts;

service facts {
    rpc CreateFact(Fact) returns (FactID);
}

message FactID {
    int64 Fact = 1;
}

message Fact{
    string Fact = 1;
}

